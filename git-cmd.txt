git init - to create repository
git status -checking the status of the file 
   status can be :
               untracked
               staged
               tracked

git add 'file-name': adding file into staged

git commit:commit the file

git restore :restore the deleted file (possible if its commited)

its warking purfect
